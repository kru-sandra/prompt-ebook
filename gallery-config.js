/* ================== CONFIG ตั้งค่าระบบ ================== */

// รูปแรกคือ p001
const START = 1;

// 📌 ตอนนี้มีรูปจริงถึง p084
// ถ้าเพิ่ม เช่น p110 → เปลี่ยนเป็น 110 ตรงนี้ "ที่เดียวพอ"
const END = 84;

// จำนวนรูปต่อหน้า
const PER_PAGE = 20;

// จำนวนหน้าทั้งหมด (คำนวนอัตโนมัติ)
const TOTAL_PAGES = Math.ceil(END / PER_PAGE);


/* ================== EXPORT (ใช้ในหลายไฟล์) ================== */
window.galleryConfig = {
  START,
  END,
  PER_PAGE,
  TOTAL_PAGES
};

